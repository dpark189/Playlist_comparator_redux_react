# Playlist_comparator_redux_react
small redux-react app that compares spotify and appleMusic playlists for matching tracks

### to test
* download files
* npm install
* npm run webpack
* open the index.html file in the root directory
